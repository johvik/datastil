language: node_js
node_js:
  - "0.10"
  - "0.8"

before_install:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start

before_script:
  - npm install -g grunt-cli karma
  - cd server
  - npm install
  - cd ../client
  - npm install
  - cd ..

script: make travis

